# 西湖大学理学院官网管理后台 — PRD

> 版本：v1.0 | 更新日期：2026-08-11 | 作者：Shirley / Claw

---

## 一、项目概述

### 1.1 项目背景

西湖大学理学院下设理学院、天文系、化学系三个学科单元，各自拥有独立官网（PC 端 + H5 手机端）。目前缺少统一的内容管理后台，各单元内容维护分散、权限管理粗放。

因此需要构建一个**多租户统一管理后台**，实现：
- 同一套后台管理三个独立官网（理学院 / 天文系 / 化学系）
- 按角色控制租户访问权限，登录后可自由切换有权限的租户
- 新闻、活动等公共内容由理学院统一创建，按策略下发到天文系、化学系
- 各租户可独立管理自有内容（师资、科研、招生等）

### 1.2 设计目标

- **多租户统一管理**：一套后台管理三个官网，租户间数据隔离
- **灵活权限控制**：RBAC 模型，角色绑定菜单权限 + 租户范围
- **内容统一下发**：理学院创建的新闻/活动可一键分发到天文系、化学系
- **租户快速切换**：顶栏租户选择器，一键切换上下文
- **PC + H5 双端发布**：后台编辑内容自动同步到 PC 端和 H5 端官网

### 1.3 技术方案

| 维度 | 方案 |
|------|------|
| 架构形态 | 前后端分离，SPA 单页应用（Vue3 + Element Plus） |
| 后端 | Java Spring Boot / Go Gin，RESTful API |
| 数据持久化 | MySQL 8.0，Redis 缓存 |
| 权限模型 | RBAC（用户 → 角色 → 菜单权限 + 租户范围） |
| 多租户方案 | 共享数据库 + 租户 ID 隔离（tenant_id 字段） |
| 前端发布 | Nginx 静态部署，API 网关统一代理 |
| 官网客户端 | PC 端自适应 + H5 移动端响应式 |

### 1.4 关键页面

- **登录页**：账号密码登录，登录后根据角色进入默认租户
- **仪表盘**：进入后台后的首页概览（各租户内容统计）
- **系统管理**：用户管理 / 角色管理 / 菜单管理 / 租户管理
- **内容管理**（后续迭代）：新闻管理、活动管理、师资管理、页面管理等

---

## 二、功能模块

### 2.1 租户切换（全局功能）

| 功能点 | 说明 |
|--------|------|
| 租户选择器 | 顶栏右上角下拉框，列出当前用户有权限的所有租户 |
| 切换租户 | 选择后立即切换上下文，左侧菜单、数据范围均按新租户刷新 |
| 默认租户 | 登录后自动进入用户所关联的第一个租户 |
| 租户标识 | 切换后顶栏显示当前租户名称 + 主题色标识 |

**业务规则：**
- 仅显示当前用户有权限的租户列表
- 切换租户不改变登录态，仅改变数据上下文
- 系统管理员（超管）可见全部租户

### 2.2 用户管理

| 功能点 | 说明 |
|--------|------|
| 列表 | 展示用户名、姓名、所属租户、角色、状态、创建时间，支持搜索与分页 |
| 新增 | 填写用户名、密码、姓名、邮箱、手机号，分配角色与租户 |
| 编辑 | 修改姓名、邮箱、手机号、角色、租户范围；不可修改用户名 |
| 删除 | 软删除（禁用），保留数据关联；超管账号不可删除 |
| 启用/禁用 | 切换用户状态，禁用后无法登录 |
| 重置密码 | 管理员重置用户密码为初始密码 |

**字段表：**

| 字段 | 类型 | 必填 | 约束 |
|------|------|------|------|
| 用户名 | 文本 | 是 | 唯一，4-32 字符，字母开头 |
| 密码 | 文本 | 是 | 8-32 字符，含大小写+数字 |
| 姓名 | 文本 | 是 | 2-20 字符 |
| 邮箱 | 文本 | 否 | 邮箱格式 |
| 手机号 | 文本 | 否 | 11 位数字 |
| 所属角色 | 多选 | 是 | 关联角色表 |
| 所属租户 | 多选 | 是 | 关联租户表 |
| 状态 | 枚举 | — | 启用 / 禁用，默认启用 |

**业务规则：**
- 用户名全局唯一
- 一个用户可分配多个角色和多个租户
- 删除用户为软删除，保留所有操作日志关联
- 超管账号（admin）不可删除、不可禁用

### 2.3 角色管理

| 功能点 | 说明 |
|--------|------|
| 列表 | 展示角色名称、角色标识、描述、创建时间 |
| 新增 | 填写角色名称、标识、描述，勾选菜单权限、设置租户范围 |
| 编辑 | 修改名称、描述、菜单权限、租户范围 |
| 删除 | 未被任何用户引用的角色可删除 |
| 权限分配 | 树形勾选菜单权限，支持全选/反选 |

**字段表：**

| 字段 | 类型 | 必填 | 约束 |
|------|------|------|------|
| 角色名称 | 文本 | 是 | 唯一，2-32 字符 |
| 角色标识 | 文本 | 是 | 唯一，大写字母+下划线，如 SCIENCE_ADMIN |
| 描述 | 文本 | 否 | 最多 200 字符 |
| 菜单权限 | JSON | 是 | 存储菜单 ID 数组 |
| 租户范围 | JSON | 是 | 存储租户 ID 数组 |

**业务规则：**
- 角色标识全局唯一，用于前端权限判断
- 角色删除前需校验是否有用户关联
- 默认内置角色：超管、理学院管理员、天文系管理员、化学系管理员、内容编辑

### 2.4 菜单管理

| 功能点 | 说明 |
|--------|------|
| 树形列表 | 以树形展示全部菜单，支持展开/折叠 |
| 新增菜单 | 选择父级菜单，填写名称、路由、图标、排序 |
| 编辑菜单 | 修改名称、路由、图标、排序、显示/隐藏 |
| 删除菜单 | 无子菜单且无角色关联方可删除 |
| 排序 | 支持拖拽排序或手动输入序号 |

**字段表：**

| 字段 | 类型 | 必填 | 约束 |
|------|------|------|------|
| 菜单名称 | 文本 | 是 | 2-20 字符 |
| 父级菜单 | 下拉 | 否 | 自关联，null=一级菜单 |
| 路由路径 | 文本 | 否 | 如 /system/user，一级目录可为空 |
| 组件路径 | 文本 | 否 | 前端组件路径 |
| 图标 | 文本 | 否 | Element Plus 图标名 |
| 排序号 | 数字 | 是 | 整数，越小越靠前 |
| 是否可见 | 枚举 | — | 显示 / 隐藏，默认显示 |
| 类型 | 枚举 | 是 | 目录 / 菜单 / 按钮 |

**业务规则：**
- 一级菜单类型为「目录」，二级为「菜单」，三级为「按钮」
- 菜单路径唯一
- 删除目录时需级联检查子菜单及角色关联

### 2.5 租户管理

| 功能点 | 说明 |
|--------|------|
| 列表 | 展示租户名称、租户标识、官网域名、联系人、状态 |
| 新增 | 填写租户名称、标识、官网 PC/H5 域名、主题色、Logo |
| 编辑 | 修改租户信息、域名、主题配置 |
| 删除 | 租户下有用户/内容数据时不可删除 |
| 配置 | 设置租户主题色、Logo、SEO 信息 |

**字段表：**

| 字段 | 类型 | 必填 | 约束 |
|------|------|------|------|
| 租户名称 | 文本 | 是 | 唯一，如"理学院" |
| 租户标识 | 文本 | 是 | 唯一，如 science |
| PC 域名 | 文本 | 否 | 如 science.westlake.edu.cn |
| H5 域名 | 文本 | 否 | 如 m-science.westlake.edu.cn |
| 主题色 | 文本 | 否 | 十六进制色值 |
| Logo | 图片 | 否 | PNG/SVG，≤500KB |
| 联系人 | 文本 | 否 | — |
| 联系电话 | 文本 | 否 | — |
| 状态 | 枚举 | — | 启用 / 禁用 |

**业务规则：**
- 租户标识全局唯一
- 预置三租户：理学院、天文系、化学系
- 理学院为「主租户」，新闻/活动可下发至子租户
- 禁用租户后，该租户下所有用户无法登录

### 2.6 Banner管理

| 功能点 | 说明 |
|--------|------|
| 列表 | 展示缩略图、标题、跳转链接、适用设备(PC/H5)、排序号、状态 |
| 新增 | 上传图片(PC/H5双尺寸)、填写标题、跳转链接、选择适用设备 |
| 编辑 | 修改标题、链接、替换图片、调整设备适配 |
| 排序 | 拖拽排序或手动输入序号，影响前台轮播顺序 |
| 启用/禁用 | 控制Banner是否在前台展示 |

**字段表：**

| 字段 | 类型 | 必填 | 约束 |
|------|------|------|------|
| 标题 | 文本 | 是 | 2-50 字符 |
| PC端图片 | 图片 | 是 | 1920×600px，≤1MB |
| H5端图片 | 图片 | 是 | 750×400px，≤500KB |
| 跳转链接 | 文本 | 否 | URL格式 |
| 适用设备 | 多选 | 是 | PC / H5 |
| 排序号 | 数字 | 是 | 整数，越小越靠前 |
| 状态 | 枚举 | — | 启用 / 禁用 |

**业务规则：**
- 同一设备最多展示 5 个Banner
- 排序号唯一（同设备内）
- 图片自动压缩，PC端≤1MB、H5端≤500KB

### 2.7 学科介绍

| 功能点 | 说明 |
|--------|------|
| 列表 | 展示学科名称、所属院系、简介摘要、状态、更新时间 |
| 新增 | 填写学科名称、所属院系、详细介绍（富文本） |
| 编辑 | 修改学科信息和介绍内容 |
| 删除 | 支持软删除，已发布内容可撤回草稿 |
| 预览 | 弹窗预览前台展示效果 |

**字段表：**

| 字段 | 类型 | 必填 | 约束 |
|------|------|------|------|
| 学科名称 | 文本 | 是 | 唯一，2-50 字符 |
| 所属院系 | 下拉 | 是 | 关联租户（理学院/天文系/化学系） |
| 封面图 | 图片 | 否 | — |
| 详细介绍 | 富文本 | 是 | HTML内容 |
| 状态 | 枚举 | — | 草稿 / 已发布 |

**业务规则：**
- 学科名称在同一租户下唯一
- 各租户仅能管理本租户的学科介绍

### 2.8 教师队伍（内容管理下）

| 功能点 | 说明 |
|--------|------|
| 列表 | 展示姓名、职称、研究方向、所属院系、排序、状态 |
| 新增 | 填写姓名、职称、研究方向、个人简介、头像 |
| 编辑 | 修改教师信息、头像、简介 |
| 排序 | 按职称+排序号排列展示 |
| 详情 | 查看/编辑教师完整信息页 |

**字段表：**

| 字段 | 类型 | 必填 | 约束 |
|------|------|------|------|
| 姓名 | 文本 | 是 | 2-20 字符 |
| 职称 | 枚举 | 是 | 教授/副教授/助理教授/讲师 |
| 研究方向 | 文本 | 是 | — |
| 所属院系 | 下拉 | 是 | 关联租户 |
| 个人简介 | 文本 | 否 | 最多 500 字符 |
| 头像 | 图片 | 否 | — |
| 排序号 | 数字 | 是 | 越小越靠前 |
| 状态 | 枚举 | — | 展示 / 隐藏 |

### 2.9 招生信息

| 功能点 | 说明 |
|--------|------|
| 列表 | 展示标题、分类、适用年份、状态、更新时间 |
| 新增 | 填写标题、分类、内容（富文本）、适用年份 |
| 编辑 | 修改内容和元信息 |
| 分类 | 招生简章 / 专业目录 / 常见问题 |

**字段表：**

| 字段 | 类型 | 必填 | 约束 |
|------|------|------|------|
| 标题 | 文本 | 是 | 2-100 字符 |
| 分类 | 枚举 | 是 | 招生简章/专业目录/常见问题 |
| 内容 | 富文本 | 是 | HTML内容 |
| 适用年份 | 文本 | 是 | 如 2026 |
| 状态 | 枚举 | — | 草稿 / 已发布 |

**业务规则：**
- 同一分类+年份下可有多条内容
- 各租户独立管理本租户招生信息

### 2.10 学院介绍

| 功能点 | 说明 |
|--------|------|
| 编辑 | 单页面富文本编辑，含标题、副标题、正文 |
| 图片 | 支持图文混排，图片上传 |
| 保存 | 即时保存，自动记录版本 |

**字段表：**

| 字段 | 类型 | 必填 | 约束 |
|------|------|------|------|
| 页面标题 | 文本 | 是 | — |
| 副标题 | 文本 | 否 | — |
| 内容 | 富文本 | 是 | HTML内容 |

**业务规则：**
- 每个租户仅有一条学院介绍记录（单页编辑）

### 2.11 科系介绍

| 功能点 | 说明 |
|--------|------|
| 列表 | 展示科系名称、所属租户、负责人、状态 |
| 新增/编辑 | 填写科系名称、介绍内容、负责人 |
| 删除 | 支持软删除 |

**字段表：**

| 字段 | 类型 | 必填 | 约束 |
|------|------|------|------|
| 科系名称 | 文本 | 是 | 唯一（同租户内） |
| 所属租户 | 下拉 | 是 | — |
| 负责人 | 文本 | 否 | — |
| 介绍内容 | 富文本 | 是 | — |

### 2.12 学院生活

| 功能点 | 说明 |
|--------|------|
| 列表 | 展示标题、类型、封面图数量、状态 |
| 新增 | 填写标题、类型、上传图片集、描述 |
| 编辑 | 修改内容和图片 |

**字段表：**

| 字段 | 类型 | 必填 | 约束 |
|------|------|------|------|
| 标题 | 文本 | 是 | — |
| 类型 | 枚举 | 是 | 活动风采/校园风光/学生活动 |
| 描述 | 文本 | 否 | — |
| 图片集 | 多图 | 是 | 最多 20 张 |
| 状态 | 枚举 | — | 草稿 / 已发布 |

### 2.13 联系我们

| 功能点 | 说明 |
|--------|------|
| 编辑 | 单页表单，含地址、电话、邮箱、办公时间、地图 |

**字段表：**

| 字段 | 类型 | 必填 | 约束 |
|------|------|------|------|
| 地址 | 文本 | 是 | — |
| 联系电话 | 文本 | 是 | — |
| 电子邮箱 | 文本 | 是 | 邮箱格式 |
| 办公时间 | 文本 | 否 | — |
| 地图代码 | 文本 | 否 | iframe嵌入代码 |

**业务规则：**
- 每个租户仅一条联系信息（单页编辑）

### 2.14 教师队伍（独立一级菜单）

| 功能点 | 说明 |
|--------|------|
| 列表 | 同 2.8，面向官网前台展示，支持头衔字段 |
| 筛选 | 按职称、院系筛选 |

**字段表：** 同 2.8 教师队伍，额外增加「头衔」字段（国家杰青/长江学者/优青等）。

> 说明：2.8 与 2.14 操作同一数据源，2.8 侧重内容管理（增删改查），2.14 侧重前台展示排序与头衔管理。

### 2.15 新闻动态

| 功能点 | 说明 |
|--------|------|
| 列表 | 展示标题、分类、下发目标、状态、发布时间 |
| 新增 | 填写标题、分类、内容、封面图，选择下发目标租户 |
| 下发 | 理学院创建时可勾选「同步到天文系/化学系」 |
| 撤回 | 已发布的新闻可撤回至草稿状态 |

**字段表：**

| 字段 | 类型 | 必填 | 约束 |
|------|------|------|------|
| 标题 | 文本 | 是 | 2-100 字符 |
| 分类 | 枚举 | 是 | 科研动态/学院新闻/通知公告 |
| 内容 | 富文本 | 是 | — |
| 封面图 | 图片 | 否 | — |
| 下发目标 | 多选 | 否 | 仅主租户可选子租户 |
| 状态 | 枚举 | — | 草稿 / 已发布 |

**业务规则：**
- 仅理学院（主租户）可将新闻下发至天文系、化学系
- 下发后子租户自动获得一份副本（source_tenant_id 标记来源）
- 子租户可在自己后台查看/隐藏（不可编辑）下发的新闻
- 同一租户内不允许重复下发同一篇新闻

### 2.16 活动信息

| 功能点 | 说明 |
|--------|------|
| 列表 | 展示活动名称、类型、活动时间、下发目标、状态 |
| 新增 | 填写活动名称、类型、时间、地点、内容，选择下发目标 |
| 下发 | 同新闻动态，理学院创建时可下发 |

**字段表：**

| 字段 | 类型 | 必填 | 约束 |
|------|------|------|------|
| 活动名称 | 文本 | 是 | 2-100 字符 |
| 类型 | 枚举 | 是 | 学术讲座/学术会议/校园活动 |
| 活动时间 | 日期时间 | 是 | — |
| 地点 | 文本 | 否 | — |
| 内容 | 富文本 | 是 | — |
| 下发目标 | 多选 | 否 | 仅主租户可选子租户 |
| 状态 | 枚举 | — | 草稿/预告中/已发布 |

**业务规则：**
- 同新闻动态的下发规则
- 已过期的活动自动标记「已结束」

## 三、权限模型

### 3.1 数据关系

```
用户 ──N:M──→ 角色 ──N:M──→ 菜单权限
  │                          │
  │                          └── 目录 / 菜单 / 按钮 三级
  │
  └──N:M──→ 租户（用户可访问哪些租户）
```

### 3.2 权限校验流程

1. 用户登录 → 后端返回 JWT Token + 用户信息（含角色列表、租户列表）
2. 前端根据当前选中租户 + 角色，获取该租户下的菜单权限树
3. 前端渲染左侧导航（仅显示有权限的菜单项）
4. 每次路由切换，前端路由守卫校验是否有访问权限
5. 每次 API 请求，后端中间件校验 Token + 租户上下文 + 操作权限

### 3.3 角色权限矩阵

| 功能 | 超管 | 理学院管理员 | 天文系管理员 | 化学系管理员 | 内容编辑 |
|------|:--:|:--:|:--:|:--:|:--:|
| 用户管理 | ✅ | — | — | — | — |
| 角色管理 | ✅ | — | — | — | — |
| 菜单管理 | ✅ | — | — | — | — |
| 租户管理 | ✅ | — | — | — | — |
| Banner管理 | ✅ | ✅(理学院) | ✅(天文系) | ✅(化学系) | ✅(指定租户) |
| 学科介绍 | ✅ | ✅(理学院) | ✅(天文系) | ✅(化学系) | ✅(指定租户) |
| 教师队伍(内容) | ✅ | ✅(理学院) | ✅(天文系) | ✅(化学系) | ✅(指定租户) |
| 招生信息 | ✅ | ✅(理学院) | ✅(天文系) | ✅(化学系) | ✅(指定租户) |
| 学院介绍 | ✅ | ✅(理学院) | ✅(天文系) | ✅(化学系) | ✅(指定租户) |
| 科系介绍 | ✅ | ✅(理学院) | ✅(天文系) | ✅(化学系) | ✅(指定租户) |
| 学院生活 | ✅ | ✅(理学院) | ✅(天文系) | ✅(化学系) | ✅(指定租户) |
| 联系我们 | ✅ | ✅(理学院) | ✅(天文系) | ✅(化学系) | ✅(指定租户) |
| 教师队伍(前台) | ✅ | ✅(理学院) | ✅(天文系) | ✅(化学系) | ✅(指定租户) |
| 新闻动态 | ✅ | ✅(理学院) | ✅(天文系) | ✅(化学系) | ✅(指定租户) |
| 活动信息 | ✅ | ✅(理学院) | ✅(天文系) | ✅(化学系) | ✅(指定租户) |
| 内容下发 | ✅ | ✅(理学院→子租户) | — | — | — |
| 租户切换 | 全部 | 理学院 | 天文系 | 化学系 | 指定租户 |

> 系统管理员（超管）拥有全部功能+全部租户权限；各系管理员仅能管理自己租户的内容；内容编辑仅能编辑被分配租户下的内容，无系统管理权限。

---

## 四、数据模型

### 4.1 核心表结构

```
sys_tenant          — 租户表
sys_user            — 用户表
sys_role            — 角色表
sys_menu            — 菜单表
sys_user_role       — 用户-角色关联表
sys_role_menu       — 角色-菜单关联表
sys_user_tenant     — 用户-租户关联表
sys_role_tenant     — 角色-租户关联表
cms_banner          — Banner表（租户隔离）
cms_subject         — 学科介绍表（租户隔离）
cms_teacher         — 教师队伍表（租户隔离）
cms_admission       — 招生信息表（租户隔离）
cms_about_college   — 学院介绍表（单页，租户隔离）
cms_department      — 科系介绍表（租户隔离）
cms_campus_life     — 学院生活表（租户隔离）
cms_contact         — 联系我们表（单页，租户隔离）
cms_news            — 新闻表（租户隔离，支持下发）
cms_event           — 活动表（租户隔离，支持下发）
```

### 4.2 系统管理核心表

**sys_tenant（租户表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| name | varchar(50) | 租户名称 |
| code | varchar(30) | 租户标识，唯一 |
| pc_domain | varchar(200) | PC 官网域名 |
| h5_domain | varchar(200) | H5 官网域名 |
| theme_color | varchar(20) | 主题色 |
| logo_url | varchar(500) | Logo 地址 |
| contact_name | varchar(50) | 联系人 |
| contact_phone | varchar(20) | 联系电话 |
| is_master | tinyint | 是否主租户（0/1），理学院=1 |
| status | tinyint | 0=禁用 1=启用 |
| created_at | datetime | — |
| updated_at | datetime | — |

**sys_user（用户表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| username | varchar(32) | 用户名，唯一 |
| password | varchar(200) | 加密密码 |
| real_name | varchar(20) | 姓名 |
| email | varchar(100) | 邮箱 |
| phone | varchar(20) | 手机号 |
| status | tinyint | 0=禁用 1=启用 |
| is_deleted | tinyint | 0=正常 1=已删除 |
| created_at | datetime | — |
| updated_at | datetime | — |

**sys_role（角色表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| name | varchar(32) | 角色名称，唯一 |
| code | varchar(50) | 角色标识，唯一 |
| description | varchar(200) | 描述 |
| created_at | datetime | — |
| updated_at | datetime | — |

**sys_menu（菜单表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| parent_id | bigint | 父级 ID，NULL=一级 |
| name | varchar(20) | 菜单名称 |
| type | varchar(10) | 目录/菜单/按钮 |
| path | varchar(200) | 路由路径 |
| component | varchar(200) | 组件路径 |
| icon | varchar(50) | 图标 |
| sort | int | 排序号 |
| visible | tinyint | 0=隐藏 1=显示 |
| created_at | datetime | — |
| updated_at | datetime | — |

**关联表：**

| 关联表 | 字段 | 唯一约束 |
|--------|------|----------|
| sys_user_role | user_id, role_id | (user_id, role_id) |
| sys_role_menu | role_id, menu_id | (role_id, menu_id) |
| sys_user_tenant | user_id, tenant_id | (user_id, tenant_id) |
| sys_role_tenant | role_id, tenant_id | (role_id, tenant_id) |

### 4.3 内容表（租户隔离）

**cms_news（新闻表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| tenant_id | bigint | 所属租户 |
| source_tenant_id | bigint | 来源租户（下发时记录原始租户） |
| title | varchar(200) | 标题 |
| content | longtext | 内容 |
| status | tinyint | 草稿/已发布/已下线 |
| is_distributed | tinyint | 是否已下发到子租户 |
| distributed_to | json | 下发目标租户 ID 数组 |
| created_at | datetime | — |
| updated_at | datetime | — |

> **唯一约束**：(tenant_id, source_tenant_id, title) 避免同一租户内重复下发同一篇新闻。

**cms_event（活动表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| tenant_id | bigint | 所属租户 |
| source_tenant_id | bigint | 来源租户 |
| title | varchar(200) | 活动名称 |
| type | varchar(20) | 学术讲座/学术会议/校园活动 |
| event_time | datetime | 活动时间 |
| location | varchar(200) | 地点 |
| content | longtext | 内容 |
| status | tinyint | 草稿/预告中/已发布/已结束 |
| is_distributed | tinyint | 是否已下发 |
| distributed_to | json | 下发目标租户 ID 数组 |
| created_at | datetime | — |
| updated_at | datetime | — |

**cms_banner（Banner表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| tenant_id | bigint | 所属租户 |
| title | varchar(50) | 标题 |
| pc_image | varchar(500) | PC端图片URL |
| h5_image | varchar(500) | H5端图片URL |
| link_url | varchar(500) | 跳转链接 |
| device | varchar(20) | PC/H5/PC+H5 |
| sort | int | 排序号 |
| status | tinyint | 0=禁用 1=启用 |
| created_at | datetime | — |
| updated_at | datetime | — |

**cms_subject（学科介绍表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| tenant_id | bigint | 所属租户 |
| name | varchar(50) | 学科名称 |
| cover_image | varchar(500) | 封面图 |
| content | longtext | 详细介绍（HTML） |
| status | tinyint | 草稿/已发布 |
| created_at | datetime | — |
| updated_at | datetime | — |

> **唯一约束**：(tenant_id, name) 同租户下学科名称唯一。

**cms_teacher（教师队伍表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| tenant_id | bigint | 所属租户 |
| name | varchar(20) | 姓名 |
| title | varchar(20) | 职称（教授/副教授/助理教授/讲师） |
| honor | varchar(50) | 头衔（国家杰青/长江学者等） |
| research_area | varchar(200) | 研究方向 |
| avatar | varchar(500) | 头像URL |
| bio | varchar(500) | 个人简介 |
| sort | int | 排序号 |
| status | tinyint | 0=隐藏 1=展示 |
| created_at | datetime | — |
| updated_at | datetime | — |

**cms_admission（招生信息表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| tenant_id | bigint | 所属租户 |
| title | varchar(100) | 标题 |
| category | varchar(20) | 招生简章/专业目录/常见问题 |
| content | longtext | 内容（HTML） |
| year | varchar(10) | 适用年份 |
| status | tinyint | 草稿/已发布 |
| created_at | datetime | — |
| updated_at | datetime | — |

**cms_about_college（学院介绍表，单页）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| tenant_id | bigint | 所属租户，唯一 |
| title | varchar(50) | 页面标题 |
| subtitle | varchar(100) | 副标题 |
| content | longtext | 内容（HTML） |
| updated_at | datetime | — |

> **唯一约束**：(tenant_id) 每个租户仅一条记录。

**cms_department（科系介绍表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| tenant_id | bigint | 所属租户 |
| name | varchar(50) | 科系名称 |
| director | varchar(20) | 负责人 |
| content | longtext | 介绍内容（HTML） |
| status | tinyint | 草稿/已发布 |
| created_at | datetime | — |
| updated_at | datetime | — |

> **唯一约束**：(tenant_id, name) 同租户下科系名称唯一。

**cms_campus_life（学院生活表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| tenant_id | bigint | 所属租户 |
| title | varchar(100) | 标题 |
| type | varchar(20) | 活动风采/校园风光/学生活动 |
| description | varchar(500) | 描述 |
| images | json | 图片URL数组，最多20张 |
| status | tinyint | 草稿/已发布 |
| created_at | datetime | — |
| updated_at | datetime | — |

**cms_contact（联系我们表，单页）**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| tenant_id | bigint | 所属租户，唯一 |
| address | varchar(200) | 地址 |
| phone | varchar(20) | 联系电话 |
| email | varchar(100) | 电子邮箱 |
| office_hours | varchar(50) | 办公时间 |
| map_code | text | 地图嵌入代码 |
| updated_at | datetime | — |

> **唯一约束**：(tenant_id) 每个租户仅一条记录。

---

## 五、UI 布局

### 5.1 整体结构

```
┌─────────────────────────────────────────────────────┐
│  顶栏 (60px)          [理学院 ▼]  [🔔] [👤 admin]   │
│  Logo + 当前租户标签                                   │
├───────────┬─────────────────────────────────────────┤
│           │                                         │
│  侧栏     │  内容区                                  │
│ (240px)   │  ┌─ 面包屑导航 ──────────────────────┐  │
│           │  │ 内容管理 > Banner管理              │  │
│  🏠 仪表盘 │  ├──────────────────────────────────┤  │
│           │  │                                   │  │
│  📝 内容   │  │  表格/表单区域                     │  │
│  管理  ▼   │  │                                   │  │
│   Banner   │  │                                   │  │
│   学科介绍  │  │                                   │  │
│   教师队伍  │  │                                   │  │
│   招生信息  │  │                                   │  │
│           │  │                                   │  │
│  🏛 学院   │  │                                   │  │
│  概况      │  │                                   │  │
│           │  │                                   │  │
│  👨‍🏫 教师  │  │                                   │  │
│  队伍      │  │                                   │  │
│           │  │                                   │  │
│  📰 新闻   │  │                                   │  │
│  活动      │  │                                   │  │
│           │  │                                   │  │
│  ⚙️ 系统   │  │                                   │  │
│  管理      │  │                                   │  │
└───────────┴─────────────────────────────────────┘
```

### 5.2 交互说明

- **左侧菜单**：系统管理默认展开，点击子菜单切换内容区
- **顶栏租户切换**：下拉选择框，切换后左侧菜单和数据范围同步刷新
- **表格操作**：行内操作按钮（编辑/删除），批量操作栏位于表格上方
- **弹窗表单**：新增/编辑使用 Dialog 弹窗，提交时前端校验 + 后端二次校验
- **面包屑**：自动根据菜单层级生成，支持点击回退
- **顶栏通知**：铃铛图标显示下发状态等通知

---

## 六、核心业务规则汇总

| 编号 | 规则 | 校验层级 |
|------|------|:--:|
| R01 | 用户名全局唯一 | 保存时 |
| R02 | 角色标识全局唯一 | 保存时 |
| R03 | 租户标识全局唯一 | 保存时 |
| R04 | 菜单路径全局唯一 | 保存时 |
| R05 | 删除用户为软删除，保留关联数据 | 删除时 |
| R06 | 有用户关联的角色不可删除 | 删除时 |
| R07 | 有子菜单或有角色关联的菜单不可删除 | 删除时 |
| R08 | 有用户/内容数据的租户不可删除 | 删除时 |
| R09 | 用户仅可切换自己有权限的租户 | 查询时 |
| R10 | 租户切换后，所有数据查询需带 tenant_id 过滤 | 查询时 |
| R11 | 理学院（主租户）可将新闻/活动下发至天文系、化学系 | 保存时 |
| R12 | 同一租户内不允许重复下发同一篇新闻 | 保存时 |
| R13 | 密码长度 8-32 字符，需含大小写字母+数字 | 前端+保存时 |
| R14 | 超管账号不可删除、不可禁用 | 前端+后端 |
| R15 | 禁用租户后，该租户下所有用户不可登录 | 登录时 |
| R16 | 同一设备 Banner 最多 5 个，排序号唯一 | 保存时 |
| R17 | 学科名称在同一租户下唯一 | 保存时 |
| R18 | 科系名称在同一租户下唯一 | 保存时 |
| R19 | 学院介绍/联系我们每租户仅一条记录 | 保存时 |
| R20 | 学院生活图片集最多 20 张 | 保存时 |
| R21 | Banner图片PC端≤1MB、H5端≤500KB | 上传时 |
| R22 | 仅主租户（理学院）可将新闻/活动下发至子租户 | 保存时 |

---

## 七、预置账号

| 登录名 | 密码 | 角色 | 租户范围 | 说明 |
|--------|------|------|----------|------|
| admin | Admin@123456 | 超管 | 全部 | 系统超级管理员 |
| science_admin | Science@123 | 理学院管理员 | 理学院 | 管理学院官网内容 |
| astro_admin | Astro@123 | 天文系管理员 | 天文系 | 管理天文系官网内容 |
| chem_admin | Chem@123 | 化学系管理员 | 化学系 | 管理化学系官网内容 |
| editor_zhang | Edit@123 | 内容编辑 | 理学院 | 内容编辑，仅有新闻/活动权限 |

---

## 八、非功能性需求

| 类别 | 要求 |
|------|------|
| 浏览器兼容 | Chrome 90+ / Edge 90+ / Firefox 100+ / Safari 15+ |
| 响应式 | 桌面端优先，管理后台最小宽度 1280px（官网客户端支持 PC + H5 双端） |
| 数据安全 | 密码 BCrypt 加密存储；JWT Token 过期时间 2h；API 防 SQL 注入/XSS |
| 性能 | 列表页首屏加载 < 1s；租户切换 < 300ms；API 响应 P95 < 200ms |
| 可用性 | 系统可用性 ≥ 99.9%；支持优雅降级 |
| 日志 | 关键操作（增删改）记录操作日志（操作人、时间、IP、内容） |
| 国际化 | 一期仅支持中文，预留 i18n 扩展能力 |

---

> **下一步**：确认 PRD 后，可进入 UI 设计稿绘制和数据库建表阶段，并可对接 test-case-generator 生成测试用例。
